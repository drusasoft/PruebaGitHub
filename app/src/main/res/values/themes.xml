<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.PruebaGitHub" parent="Theme.MaterialComponents.Light.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <!-- Estilo de la Toolbar -->
    <style name="miToolBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="miToolbarTextStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="fontFamily">@font/orbitron_black</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="miToolbarPopupTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/purple_500</item>
        <item name="fontFamily">@font/orbitron_black</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- Fin Estilo de la Toolbar -->

</resources>